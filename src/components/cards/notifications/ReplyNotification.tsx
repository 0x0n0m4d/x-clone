'use client';

import { DataNotification } from '@/interfaces/notifications.interface';

interface Props {
  dataNotification: DataNotification;
}

const ReplyNotification = ({ dataNotification }: Props) => {
  return <div>Reply Notifications</div>;
};

export default ReplyNotification;
